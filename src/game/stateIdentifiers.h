#ifndef STATE_IDENTIFIERS_H
#define STATE_IDENTIFIERS_H

class StateIdentifiers {
    public:
        enum States {
            Title,
            Menu,
            Game,
            Pause
        };
};

#endif